# Copy this file to deploy/.env on the server and replace every value.
ORACLE_PASSWORD=replace_with_a_random_hex_password
CAMPUS_DB_PASSWORD=replace_with_a_different_random_hex_password
TOKEN_SIGNING_KEY=replace_with_a_random_hex_signing_key
